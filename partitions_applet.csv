# Partition table for the applet-device example.
#
# Single factory app + NVS + applet partition for WASM binaries.
# Applets are pushed from the hub and stored in flash for XIP.
# No OTA needed because the firmware is static; only the applet changes.
#
# Name,   Type, SubType,  Offset,   Size,   Flags
nvs,      data, nvs,      0x9000,   0x6000,
phy_init, data, phy,      0xf000,   0x1000,
factory,  app,  factory,  0x10000,  0x180000,
applet,   data, fat,      0x190000, 0x60000,
storage,  data, fat,      0x1F0000, 0x10000,
